// Create a function that takes a boolean variable flag and returns it as a string.

function convertFlagToString(flag) {
    return flag ? 'true' : 'false';
}

var result = convertFlagToString(hello);
var hello = typeof(result); // checks
console.log(hello);