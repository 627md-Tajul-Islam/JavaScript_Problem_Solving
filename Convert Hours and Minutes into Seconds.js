// Write a function that takes two integers (hours, minutes), converts them to seconds, and adds them.

function convertToSeconds(hours, minutes) {
    return (hours * 3600) + (minutes * 60);
  }
  var result = convertToSeconds(1,20);
  console.log(result)
