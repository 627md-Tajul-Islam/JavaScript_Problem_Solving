//Write a function that takes an integer minutes and converts it to seconds
//Don't forget to return the result.

function MinutesIntoSeconds(minutes){
    return minutes * 60;
}
var time = MinutesIntoSeconds(10);
console.log(time + " " + "Seconds");